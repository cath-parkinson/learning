library(devtools)
library(dplyr)
library(ggplot2)
library(shiny)
install_github("StatsWithR/statsr")

#1. F= 50% of houses in Ames are smaller than 1,499.69 square feet.
#2. Sample size of 1000
#3. 25
#4. Each element represents a mean square footage from a simple random sample of 10 houses
#5. Decreases
#6. F = The variability of the sampling distribution with the smaller sample size (sample_means50) is smaller than the variability of the sampling distribution with the larger sample size (sample_means150).


a <- c(1,2,3)
length(a)
